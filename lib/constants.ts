/**
 * z-index for modal-level overlays (dialog, sheet, select, tooltip, dropdown).
 * The admin layout uses `fixed inset-0 z-[1000]`, so modals must sit above it.
 * Tailwind class: `z-[1001]`
 */
export const Z_INDEX_MODAL = 1001;
export const Z_INDEX_MODAL_CLASS = 'z-[1001]';
